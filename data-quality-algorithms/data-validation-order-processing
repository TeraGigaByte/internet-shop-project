@startuml
title Валидация данных при обработке заказов

|Система|
start
:Проверка статуса заказа;
if (Заказ не доставлен?) then (да)
  :Проверка соответствия информации о заказе и доставке;
  if (Информация корректна?) then (да)
    :Данные валидны;
  else (нет)
    :Сообщение об ошибке (некорректная информация о заказе или доставке);
  endif
else (нет)
  :Сообщение об ошибке (заказ уже доставлен, изменения невозможны);
endif
stop
@enduml
